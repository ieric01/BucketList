<br>
<br>
<br>
<br>
<br>
<br>

<% @attendees.each do |attendee| %>

	<img src="<%= attendee.user_img_url %>">
		<% if current_user == attendee %>
	  	<%= link_to " #{attendee.name} (me)", "/see_user_list/#{attendee.name}"  %></br>
	  <% else %>
	  	<%= link_to attendee.name, "/see_user_list/#{attendee.name}" %>
	  <% end %>
<% end %>

<br>
<br>
<br>
